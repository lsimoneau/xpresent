<h2><%= @deck.name %></h2>

<%= for slide <- @deck.slides do %>
  <section class="slide-content">
    <%= slide.content %>
  </section>
<% end %>

<%= link "Add a slide", to: deck_slide_path(@conn, :new, @deck) %>
